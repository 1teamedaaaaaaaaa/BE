name: PR CI

on:
  pull_request:
    branches: [ develop ]

jobs:
  placeholder-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder CI check
        run: echo "Temporary CI passed"
